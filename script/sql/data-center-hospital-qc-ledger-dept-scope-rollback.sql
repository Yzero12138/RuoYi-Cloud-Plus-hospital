-- ========================================================
-- 医院数据质控中心-科室维度改造回滚脚本
-- 文件名: data-center-hospital-qc-ledger-dept-scope-rollback.sql
-- 说明: 回滚科室维度改造，恢复原始表结构
-- ========================================================

-- --------------------------------------------------------
-- 1. 恢复台账项表唯一索引
-- --------------------------------------------------------
ALTER TABLE `hospital_qc_ledger_item`
    DROP INDEX `uk_ledger_code_dept`,
    ADD UNIQUE KEY `uk_ledger_code` (`ledger_code`);

-- --------------------------------------------------------
-- 2. 删除台账项表科室字段
-- --------------------------------------------------------
ALTER TABLE `hospital_qc_ledger_item`
    DROP COLUMN `dept_id`,
    DROP COLUMN `dept_name`,
    DROP INDEX `idx_dept_id`,
    DROP INDEX `idx_dept_status_deleted`;

-- --------------------------------------------------------
-- 3. 删除台账查询配置表字段映射配置
-- --------------------------------------------------------
ALTER TABLE `hospital_qc_ledger_query`
    DROP COLUMN `detail_field_mapping`;

-- --------------------------------------------------------
-- 注意：
-- 1. 此脚本用于回滚科室维度改造
-- 2. 执行前请确保已备份数据
-- 3. 删除字段后数据不可恢复
-- --------------------------------------------------------
